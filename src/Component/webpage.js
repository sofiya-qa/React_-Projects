document.addEventListener("DOMContentLoaded", function () {
    const button = document.getElementById("viewDashboardButton");

    button.addEventListener("click", function () {
        alert("Navigating to the Dashboard !");
    });
});
